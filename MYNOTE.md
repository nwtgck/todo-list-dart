# My Note

## Run develop server

```bash
cd <this repo>
pub get
pub serve
```


## Deployment

Deploy by the following command.  
NOTE: Install `gh-pages` by `npm install -g gh-pages` before deployment. 

```bash
cd <this repo>
./deploy.sh 
```
